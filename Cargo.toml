[package]
name = "asyncLM"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib2.rs"
crate-type = ["cdylib"]

[dependencies]
inferlet = { path = "../sdk/rust/inferlet" }
inferlet-macros = { path = "../sdk/rust/inferlet-macros" }

[profile.release]
lto = true
